[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py
# Only run Django DB tests when marked with django_db
addopts = --reuse-db --nomigrations
# Required marker to ensure test databases are created
markers =
    django_db: mark test as needing a database
